<?= $this->headLink()->appendStylesheet('/css/peer-comparison.css') ?>


<h2>Peer Comparison Report</h2>

<p>
    <strong>Report year:</strong> <?= $report['year'] ?>
</p>

<p>
    <strong>Home institution:</strong> <?= $report['currentCollege'] ?>
</p>

<p>
    <strong>Peer institutions:</strong> <?= implode(', ', $report['colleges']) ?>
</p>

<?php if ($name = $peerGroup->getName()): ?>
<p>
    <strong>Peer Group:</strong> <?= $name ?>
</p>
<?php endif ?>

<div class="container-fluid">
    <?php foreach ($report['sections'] as $section): ?>

        <div class="row-fluid">
            <div class="span12">
                <h3><?= $section['benchmark'] ?></h3>
            </div>
        </div>

        <div class="row-fluid">
            <div class="span6">
                <table class="table table-hover table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Institution</th>
                            <th>Benchmark</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($section['data'] as $institution => $value): ?>
                            <tr>
                                <td>
                                    <?= $institution ?>
                                </td>
                                <td>
                                    <?= number_format(round($value)) ?>
                                </td>
                            </tr>
                        <?php endforeach ?>
                    </tbody>
                </table>
            </div>

            <div class="span6">
                <?= $this->chart($section['chart']) ?>
            </div>
        </div>
    <?php endforeach ?>
</div>

<?php if (!empty($report['skipped'])): ?>
    <p>The following benchmarks are not included in the report because fewer than 5 or your selected peers reported data for them:</p>

    <ul>
        <?php foreach ($report['skipped'] as $benchmark): ?>
            <li><?= $benchmark ?></li>
        <?php endforeach ?>
    </ul>

<?php endif ?>

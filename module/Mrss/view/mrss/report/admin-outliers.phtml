<style>
    h2 {
        margin-top: 30px;
    }

    p a {
        font-size: 80%;
    }
</style>

<h1>Admin Outlier Report</h1>



<?php foreach ($report as $collegeInfo): ?>
    <?php
        /** @var \Mrss\Entity\College $college */
        $college = $collegeInfo['college'];

        /** @var \Mrss\Entity\Outlier[] $outliers */
        $outliers = $collegeInfo['outliers'];

        /** @var \Mrss\Entity\Issue[] $issues */
        $outliers = $collegeInfo['issues'];
    ?>

    <h2><?= $college->getName() ?></h2>

    <?php if (!$showDetails): ?>
        <p><a href="<?= $this->url('admin/outliers', array('college_id' => $college->getId())) ?>">Detailed Outlier Report</a></p>

    <?php endif ?>

    <?= $this->partial(
        'mrss/report/outliers.partial.phtml',
        array(
            'outliers' => $outliers,
            'admin' => true,
            'showDetails' => $showDetails,
            'issues' => $issues
        )
    ) ?>

<?php endforeach ?>

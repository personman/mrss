<?php /** @var \Mrss\Entity\System[] $systems */ ?>


<div class="btn-group heading-btns">
    <a class="btn btn-default" href="<?= $this->url('systems/add') ?>">
        <i class="glyphicon glyphicon-plus"></i>
        Add a <?= $this->studyConfig()->system_label ?>
    </a>
</div>
<?//= $this->config() ?>
<h1><?= ucwords($this->studyConfig()->system_label) ?>s</h1>

<?php if (!empty($systems)): ?>

    <table class="table">
        <thead>
            <th><?= ucwords($this->studyConfig()->system_label) ?></th>
            <th>Members</th>
        </thead>
        <?php foreach ($systems as $system): ?>
            <tr>
                <td>
                    <a href="<?= $this->url('systems/view', array('id' => $system->getId())) ?>">
                        <?= $system->getName() ?>
                    </a>
                </td>
                <td>
                    <?= count($system->getColleges()) ?>
                </td>
            </tr>
        <?php endforeach ?>
    </table>

<?php else: ?>

    <p>There are no <?= $this->studyConfig()->system_label ?>s yet.</p>

<?php endif ?>

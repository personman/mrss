<h2><?= $this->benchmark->getName() ?></h2>


<script type="text/javascript" src="https://www.google.com/jsapi"></script>
<script type="text/javascript">
    google.load("visualization", "1", {packages:["corechart"]});
    google.setOnLoadCallback(drawChart);
    function drawChart() {
        /*var data = google.visualization.arrayToDataTable([
            ['Year', 'Sales', 'Expenses'],
            ['2004',  1000,      400],
            ['2005',  1170,      460],
            ['2006',  660,       1120],
            ['2007',  1030,      540]
        ]);*/

        //var data = google.visualization.arrayToDataTable(<?= $this->observations
        ?>)
        var data = new google.visualization.DataTable();
        data.addColumn('number', 'Year');
        <?php foreach ($colleges as $college): ?>
            data.addColumn('number', '<?= $college->getName() ?>')
        <?php endforeach ?>
        data.addRows(<?= $this->observations ?>)

        var options = {
            title: '<?= $this->benchmark->getName() ?>'
        };

        var chart = new google.visualization.LineChart(document.getElementById('chart_div'));
        chart.draw(data, options);
    }
</script>

<div id="chart_div" style="width: 900px; height: 500px;"></div>
<?/*php foreach ($this->observations as $observation): ?>

    <?= $observation->getCollege()->getName() ?> (<?= $observation->getYear() ?>):
    <?= $observation->get($this->benchmark->getDbColumn()) ?><br>
<?php endforeach */?>

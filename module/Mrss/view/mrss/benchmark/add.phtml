<?php
$this->headScript()->appendFile(
    '/js/benchmark-form.js',
    'text/javascript'
);
$this->headScript()->appendFile(
    '/js/jquery-ui.triggeredAutocomplete.js',
    'text/javascript'
);
?>

<h2>Add Benchmark</h2>

<?php
// Add an action and submit button to the form
$form->setAttribute('action', $this->url('benchmark/add', array(
            'benchmarkGroup' => $benchmarkGroup->getId()
        )));

?>

<?= $this->ztbForm($this->form->prepare()) ?>

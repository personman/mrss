<h2>Add/Edit <?= $study->getName() ?> Module</h2>

<?php
// Add an action and submit button to the form
//$form->setAttribute('action', $this->url('sections/edit', array('study' => $study->getId(), 'id' => $section->getId())));
?>

<?= $this->ztbForm($this->form->prepare()) ?>
